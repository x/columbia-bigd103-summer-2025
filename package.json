{
  "name": "columbia-bigd103",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "slidev --base $(dirname $npm_config_slides) $npm_config_slides --port 3030",
    "export": "slidev export --per-slide --format pdf --output exports/$(basename $(dirname $npm_config_slides)).pdf $npm_config_slides"
  },
  "dependencies": {
    "@slidev/cli": "^52.0.0",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "slidev-addon-python-runner": "^0.1.3",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "playwright-chromium": "^1.54.1"
  }
}
